═══════════════════════════════════════════════════════════════
  DEPLOY SOMANSAPOS KE NETLIFY - QUICK REFERENCE
═══════════════════════════════════════════════════════════════

✅ KONFIGURASI SUDAH SIAP!
  
  📄 netlify.toml           → Build & deploy settings
  📄 public/_redirects      → SPA routing rules
  📄 NETLIFY_DEPLOY.md      → Panduan lengkap (baca ini!)

─────────────────────────────────────────────────────────────

🚀 CARA DEPLOY (PILIH SALAH SATU):

1️⃣  VIA GIT (PALING MUDAH)
   
   a. Push code ke GitHub/GitLab:
      $ git add .
      $ git commit -m "Ready for Netlify"
      $ git push origin main
   
   b. Buka: https://app.netlify.com/
   c. Klik: "Add new site" → "Import an existing project"
   d. Connect repository Anda
   e. Klik "Deploy" (settings sudah auto-detect dari netlify.toml)
   
   ✨ Done! Site live dalam 2-3 menit

─────────────────────────────────────────────────────────────

2️⃣  VIA CLI
   
   $ npm install -g netlify-cli
   $ netlify login
   $ npm run build
   $ netlify deploy --prod
   
   ✨ Done! Site URL akan muncul di terminal

─────────────────────────────────────────────────────────────

3️⃣  DRAG & DROP
   
   $ npm run build
   
   → Buka: https://app.netlify.com/drop
   → Drag folder "dist/" ke halaman
   
   ✨ Done! Instant deploy!

─────────────────────────────────────────────────────────────

📋 YANG SUDAH DIKONFIGURASI:

  ✅ Build command: npm run build
  ✅ Publish directory: dist
  ✅ Node version: 18
  ✅ SPA routing (semua route → index.html)
  ✅ Service Worker support (no-cache headers)
  ✅ Security headers (XSS, Frame, Content-Type)
  ✅ Asset caching (1 year untuk /assets/*)
  ✅ Auto SSL/HTTPS
  ✅ CDN global

─────────────────────────────────────────────────────────────

🔧 ENVIRONMENT VARIABLES (OPTIONAL):

  Di Netlify Dashboard → Site settings → Environment variables
  
  Tambahkan:
  
  VITE_API_BASE_URL=https://your-api-url.com
  VITE_APP_NAME=SomansaPOS
  VITE_APP_VERSION=1.0.0

─────────────────────────────────────────────────────────────

🌐 CUSTOM DOMAIN (OPTIONAL):

  1. Site settings → Domain management → Add custom domain
  2. Update DNS:
     - A record: 104.198.14.52
     - atau CNAME: your-site.netlify.app
  3. SSL otomatis di-provision (gratis!)

─────────────────────────────────────────────────────────────

🧪 TEST BUILD LOKAL:

  $ npm run build
  $ npm run preview
  → Buka: http://localhost:4173
  
  Test:
  - Semua routes berfungsi
  - Service Worker register
  - Offline mode works
  - No console errors

─────────────────────────────────────────────────────────────

📚 DOKUMENTASI LENGKAP:

  → NETLIFY_DEPLOY.md       (Panduan detail deploy Netlify)
  → DEPLOYMENT.md           (Panduan semua platform)
  → README.md               (Getting started)

─────────────────────────────────────────────────────────────

💡 TIPS:

  • Deploy preview untuk setiap Pull Request
  • Branch deploys untuk staging/dev
  • Rollback instant ke deploy sebelumnya
  • Build logs tersedia di dashboard
  • Form handling & Functions built-in
  • Analytics available (paid)

─────────────────────────────────────────────────────────────

🆘 TROUBLESHOOTING:

  Build gagal?
  → Check Node version (harus 18+)
  → Check netlify.toml syntax
  → Lihat build logs di dashboard
  
  Routes 404?
  → Check netlify.toml redirects
  → Check public/_redirects exists
  
  Service Worker error?
  → Check /sw.js headers (no-cache)
  → Clear browser cache

─────────────────────────────────────────────────────────────

📞 SUPPORT:

  • Netlify Docs: https://docs.netlify.com
  • Community: https://answers.netlify.com
  • Status: https://netlifystatus.com

═══════════════════════════════════════════════════════════════
  
  Happy Deploying! 🚀
  
  Your site will be live at: https://[random-name].netlify.app
  
═══════════════════════════════════════════════════════════════
